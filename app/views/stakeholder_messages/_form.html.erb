<%= form_for(@stakeholder_message) do |f| %>
  <% if @stakeholder_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stakeholder_message.errors.count, "error") %> prohibited this stakeholder_message from being saved:</h2>

      <ul>
      <% @stakeholder_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :step_id %><br>
    <%= f.number_field :step_id %>
  </div>
  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
