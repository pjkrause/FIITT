<%= form_for(@step) do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :general_message %><br>
    <%= f.text_area :general_message %>
  </div>
  <div class="field">
    <%= f.label :status_message %><br>
    <%= f.text_area :status_message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
